name: lms_app
description: CBC Learn Mate LMS
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  firebase_core: ^3.3.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.1.3
  cupertino_icons: ^1.0.8
  uuid: ^4.5.1
  flutter_quill: ^11.5.0
  image_picker: ^1.1.2
  file_picker: ^8.1.2
  video_player: ^2.9.2
  youtube_player_iframe: ^5.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true